# Infrastructure

Operational files for local and deployment environments.

- `docker/`: Dockerfiles and image build assets
- `compose/`: local multi-service orchestration
- `nginx/`: reverse proxy and TLS routing setup
- `db/`: migrations and seeds
