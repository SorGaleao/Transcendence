# Packages

Shared code consumed by apps and services.

- `game-engine/`: pure deterministic game logic
- `contracts/`: shared event/API schema definitions
- `config/`: reusable config presets
- `ui/`: optional reusable design system components
