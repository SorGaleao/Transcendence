# Services

Long-running support processes outside the core server app.

- `bot-runner/`: simulated players for stress and gameplay testing
- `worker/`: optional background jobs (analytics, cleanup, notifications)
